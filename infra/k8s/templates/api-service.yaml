apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
spec:
  type: {{ .Values.service.api.type }}
  selector:
    app: {{ .Release.Name }}-api
  ports:
    - name: http
      port: {{ .Values.service.api.port }}
      targetPort: 8000
