apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-web
spec:
  type: {{ .Values.service.web.type }}
  selector:
    app: {{ .Release.Name }}-web
  ports:
    - name: http
      port: {{ .Values.service.web.port }}
      targetPort: 3000
